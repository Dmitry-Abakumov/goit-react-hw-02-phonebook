import styled from '@emotion/styled';
import { space, typography, layout, flexbox, grid } from 'styled-system';

const Box = styled('div')(space, typography, layout, flexbox, grid);

export default Box;
